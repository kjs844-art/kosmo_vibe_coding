server:
  port: 8080

spring:
  datasource:
    # 루트 디렉토리의 .env 로부터 주입되는 환경변수를 바인딩합니다.
    url: ${DB_URL:jdbc:h2:mem:vibe;MODE=PostgreSQL}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  # .env의 JWT_SCRET 키로 서명용 비밀키를 주입받습니다.
  secret: ${JWT_SCRET}
  expiration: 86400000   # 24시간 (밀리초) - 기존 JwtTokenProvider용
  # 선생님 수업 JwtTokenManager 방식 추가
  issuer: kosmo
  accessValidTime: 60000      # accessToken: 60초 (테스트용, 실제는 15분~1시간)
  refreshValidTime: 600000    # refreshToken: 600초 (테스트용, 실제는 7일~30일)

file:
  upload-dir: ${UPLOAD_BASE_DIR:D:/upload}
  paths:
    members: members
    boards: boards

